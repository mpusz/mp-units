{{!--
    Section wrapper.  Markdown has no DOM container; passthrough.
    `name` / `id` are accepted for parity with HTML and reserved for
    future use (e.g. an `<a id="...">` anchor).
--}}
{{> @partial-block }}
